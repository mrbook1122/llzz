import React from "react";
import styled from "styled-components";

const Container = styled.div`
    width: 100%;
    height: 50px;
    position: sticky;
    background: #eee;
    z-index: 10;
    top: 0;
`

const Top = props => {
    return (
        <Container>

        </Container>
    )
}

export default Top